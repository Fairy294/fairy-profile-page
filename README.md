# Fairy Profile Page

一个静态 GitHub Pages 单页站点：Fairy 的黑黄 Inter-Knot 风格资料页。

## 本地预览

```bash
python3 -m http.server 4173
```

然后打开 `http://localhost:4173/`。

## 部署

GitHub Pages 设置为 `main` branch / root 即可。页面无外部 JS 框架，图片素材使用 `assets/fairy-bwiki.png`。
